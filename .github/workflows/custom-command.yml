name: CICD

on:
 push:
 workflow_dispatch:

jobs:
  With_Command_Input:
    uses: .\.github\workflows\test.yml
    with:
      project: test
      command: 'echo "test"'
  Without_Command_Input:
    uses: ./.github\workflows\test.yml
    with:
      project: build
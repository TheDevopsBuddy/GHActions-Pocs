name: CICD

on:
 push:
 workflow_dispatch:

jobs:
  With_Command_Input:
    uses: .\.github\workflows\test.yml@main
    with:
      project: test
      command: 'echo "test"'
  Without_Command_Input:
    uses: ./.github\workflows\test.yml@main
    with:
      project: build